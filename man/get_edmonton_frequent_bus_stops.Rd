% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/transit.R
\name{get_edmonton_frequent_bus_stops}
\alias{get_edmonton_frequent_bus_stops}
\title{Load Edmonton frequent bus network stops from GTFS}
\usage{
get_edmonton_frequent_bus_stops(
  gtfs_path,
  route_numbers = 1:9,
  service_date = NULL,
  start_time = "06:00:00",
  end_time = "21:00:00",
  freq_threshold_min = 15,
  crs = NULL
)
}
\arguments{
\item{gtfs_path}{Path to the ETS GTFS zip file.}

\item{route_numbers}{Integer vector of frequent-network route numbers.
Defaults to \code{1:9}. Matched against \code{route_short_name} as zero-padded
three-digit strings (e.g. \code{"001"}).}

\item{service_date}{A \code{Date} used to select active service IDs from the
GTFS calendar. Defaults to \code{NULL}, which uses the service ID with the
most departures (see \code{tidytransit::get_stop_frequency()}).}

\item{start_time, end_time}{Analysis window for headway calculation, as
\code{"HH:MM:SS"} strings. Default \code{"06:00:00"}-\code{"21:00:00"}.}

\item{freq_threshold_min}{Numeric headway threshold in minutes. A stop is
considered part of the frequent network if at least one route serving
it has a mean headway at or under this threshold. Default \code{15}.}

\item{crs}{CRS to transform the result to.}
}
\value{
An \code{sf} object with columns \code{stop_id}, \code{stop_name}, and point
geometry.
}
\description{
Reads an Edmonton Transit System GTFS zip, restricts trips to the given
frequent-network route numbers (routes 1-9 by default), and returns the
stops where scheduled headway meets a frequency threshold.
}
\details{
Headway is computed per route (via \code{tidytransit::get_stop_frequency()}
with \code{by_route = TRUE}), not by combining unrelated routes at a shared
stop. Route 1 branches into 1A and 1B, which share a single GTFS
\code{route_id} but run every ~30 minutes individually; per-route aggregation
automatically recombines them into ~15-minute service on their shared
trunk, while leaving branch-only stops at ~30 minutes, with no
route-specific special-casing required.
}
