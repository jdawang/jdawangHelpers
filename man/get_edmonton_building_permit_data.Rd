% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/building_permits.R
\name{get_edmonton_building_permit_data}
\alias{get_edmonton_building_permit_data}
\title{Download Edmonton General Building Permits from the Open Data SODA3 API}
\usage{
get_edmonton_building_permit_data(
  cache_dir,
  start_date = NULL,
  end_date = NULL,
  force_refresh = FALSE,
  key_id = Sys.getenv("SOCRATA_KEY_ID"),
  key_secret = Sys.getenv("SOCRATA_KEY_SECRET")
)
}
\arguments{
\item{cache_dir}{Path to a local directory for caching downloaded data.
Cache files are named by the date range, e.g.
\verb{edmonton_building_permits_2023-01-01_all.rds}.}

\item{start_date}{Optional start date (inclusive) for \code{issue_date} filtering.
A \code{Date} or ISO-8601 string. Default \code{NULL} (no lower bound).}

\item{end_date}{Optional end date (inclusive) for \code{issue_date} filtering.
A \code{Date} or ISO-8601 string. Default \code{NULL} (no upper bound).}

\item{force_refresh}{If \code{TRUE}, re-downloads even if a cached file exists.
Default \code{FALSE}.}

\item{key_id}{Socrata API key ID. Defaults to \code{SOCRATA_KEY_ID} env var.}

\item{key_secret}{Socrata API key secret. Defaults to \code{SOCRATA_KEY_SECRET}
env var.}
}
\value{
An \code{sf} object with Point geometry (CRS 4326; \code{NA} for permits
without coordinates), a \code{date_issued} \code{Date} column, and all other
columns from the dataset.
}
\description{
Fetches all records via the SODA3 GeoJSON endpoint, paginates automatically,
and caches the result to disk. The returned object has column names
compatible with \code{\link[=filter_edmonton_residential]{filter_edmonton_residential()}}, \code{\link[=add_edmonton_project_type]{add_edmonton_project_type()}},
and the rest of the pipeline — do \strong{not} pass the result to
\code{\link[=clean_edmonton_bp_columns]{clean_edmonton_bp_columns()}}.
}
