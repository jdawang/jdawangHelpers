% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/bike_lanes.R
\name{add_bike_lane_distance}
\alias{add_bike_lane_distance}
\title{Add distance-to-bike-infrastructure column}
\usage{
add_bike_lane_distance(data, bike_lanes, classification_filter = NULL)
}
\arguments{
\item{data}{An \code{sf} object.}

\item{bike_lanes}{An \code{sf} object of bike lane segments (e.g. from
\code{\link[=get_edmonton_bike_lane_data]{get_edmonton_bike_lane_data()}}), with a \code{classification} column.}

\item{classification_filter}{Character vector of \code{classification} values to
keep before computing distance. \code{NULL} (default) uses all segments.}
}
\value{
The input with a new \code{distance_from_bike_infra} numeric column
(km). The column has a \code{label} attribute for use as a default ggplot2
axis title.
}
\description{
Calculates the minimum distance in kilometres from each row to the nearest
bike lane segment in \code{bike_lanes}. Optionally filters to a subset of
\code{classification} values first (e.g. to exclude painted-lane-only
segments that aren't physically separated infrastructure).
}
