% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/bike_lanes.R
\name{get_edmonton_bike_lane_data}
\alias{get_edmonton_bike_lane_data}
\title{Download Edmonton bike lane network from the Open Data SODA3 API}
\usage{
get_edmonton_bike_lane_data(
  cache_dir,
  force_refresh = FALSE,
  key_id = Sys.getenv("SOCRATA_KEY_ID"),
  key_secret = Sys.getenv("SOCRATA_KEY_SECRET")
)
}
\arguments{
\item{cache_dir}{Path to a local directory for caching downloaded data.
Cache file is named \code{edmonton_bike_lanes.rds}.}

\item{force_refresh}{If \code{TRUE}, re-downloads even if a cached file exists.
Default \code{FALSE}.}

\item{key_id}{Socrata API key ID. Defaults to \code{SOCRATA_KEY_ID} env var.}

\item{key_secret}{Socrata API key secret. Defaults to \code{SOCRATA_KEY_SECRET}
env var.}
}
\value{
An \code{sf} object with MULTILINESTRING geometry (CRS 4326) and columns
\code{id}, \code{type}, \code{classification}, \code{network_classification},
\code{road_segment_type}, \code{duration}, \code{street_name_full}, \code{line_weight},
\code{route_coming_soon}, and \code{construction_year}.
}
\description{
Fetches all bike lane network segments via the SODA3 GeoJSON endpoint and
caches the result to disk. Unlike \code{\link[=get_edmonton_building_permit_data]{get_edmonton_building_permit_data()}},
this does not paginate: the full dataset (~10,400 rows as of 2026) fits in
a single un-paginated \verb{SELECT *} response.
}
